Image placeholders for UI:
- login_bg_placeholder.jpg (login/register background, currently black fallback)

Current behavior:
- UI still uses pure black background.
- This folder is reserved for future static imports.
